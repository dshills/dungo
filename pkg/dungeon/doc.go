// Package dungeon provides the core dungeon generator interface.
// It orchestrates the five-stage pipeline: Graph → Embed → Carve → Content → Validate.
// This package defines the primary Generator interface, configuration structures,
// and artifact types that flow through the generation pipeline.
package dungeon
